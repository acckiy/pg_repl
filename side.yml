---
  - name: Setup PostgreSQL Master
    hosts: master
    gather_facts: yes
    vars_files: hosts_info.yml
    roles:
      - pg_init_master


  - name: Setup PostgreSQL StandBy
    hosts: standby
    gather_facts: yes
    vars_files: hosts_info.yml
    roles:
      - pg_init_standby

